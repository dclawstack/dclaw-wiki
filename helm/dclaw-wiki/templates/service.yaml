apiVersion: v1
kind: Service
metadata:
  name: {{ include "dclaw-wiki.fullname" . }}
  labels:
    {{- include "dclaw-wiki.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dclaw-wiki.selectorLabels" . | nindent 4 }}
