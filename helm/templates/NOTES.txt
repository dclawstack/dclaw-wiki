{{ .Chart.Name }} deployed (release {{ .Release.Name }}, namespace {{ .Release.Namespace }}).

{{- $name := include "dclaw.name" . }}
{{- if .Values.backend.enabled }}
Backend:  {{ $name }}-backend (ClusterIP :{{ .Values.backend.service.port }})
{{- end }}
{{- if .Values.frontend.enabled }}
Frontend: {{ $name }} ({{ .Values.frontend.service.type }} :{{ .Values.frontend.service.port }}{{ if and (eq .Values.frontend.service.type "NodePort") .Values.frontend.service.nodePort }} -> nodePort {{ .Values.frontend.service.nodePort }}{{ end }})
{{- if and (eq .Values.frontend.service.type "NodePort") .Values.frontend.service.nodePort }}

Open the app at: http://localhost:{{ .Values.frontend.service.nodePort }}
{{- end }}
{{- end }}

To show this app in dpanel, merge its registry entry into the runtime ConfigMap:
  kubectl patch configmap dclaw-apps-registry -n {{ .Release.Namespace }} --type merge \
    -p "{\"data\":{\"{{ include "dclaw.appId" . }}\":$(jq -Rs . < helm/registry-entry.json)}}"
