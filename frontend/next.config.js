/** @type {import('next').NextConfig} */
const nextConfig = {
  // Full-stack Next.js app on Vercel — data + AI live in /app/api route handlers
  // (Neon + OpenRouter). No external backend proxy.
};
module.exports = nextConfig;
