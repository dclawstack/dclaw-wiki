apiVersion: v1
kind: Secret
metadata:
  name: dclaw-wiki-secret
  namespace: dclaw-core
  labels:
    app.kubernetes.io/name: dclaw-wiki
    app.kubernetes.io/part-of: dclaw-platform
type: Opaque
stringData:
  # Shared cluster PostgreSQL — one database per app (see project memory:
  # never create a per-app postgres pod).
  DATABASE_URL: "postgresql+asyncpg://postgres:postgres@dclaw-shared-postgres:5432/dclaw_wiki"
  APP_ENV: "production"
  SECRET_KEY: "dclaw-wiki-local-secret-key"
